---
interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<div
    class:list={[
        "rounded-lg border border-gray-100 bg-background-secondary p-8 shadow-md",
        className,
    ]}
>
    <slot />
</div>
